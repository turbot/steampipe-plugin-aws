[
  {
    "akas": ["{{ output.resource_aka.value }}"],
    "api_id": "{{output.resource_id.value}}",
    "created_date": "{{output.created_date.value}}",
    "name": "{{resourceName}}",
    "tags": {
      "name": "{{ resourceName }}"
    },
    "title": "{{resourceName}}"
  }
]
