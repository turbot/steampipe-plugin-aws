[
  {
    "akas": [
      "{{ output.resource_arn.value }}"
    ],
    "name": "{{ resourceName }}",
    "region": "{{ output.aws_region.value }}",
    "tags": {
      "name": "{{ resourceName }}"
    },
    "title": "{{ resourceName }}"
  }
]
