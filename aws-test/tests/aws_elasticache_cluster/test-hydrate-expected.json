[
	{
		"akas": ["{{ output.resource_aka.value }}"],
		"cache_cluster_id": "{{ output.resource_id.value }}",
		"tags": {
			"name": "{{resourceName}}"
		},
		"title": "{{ output.resource_id.value }}"
	}
]
