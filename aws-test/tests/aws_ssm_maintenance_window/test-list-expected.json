[
  {
    "name": "{{output.resource_name.value}}",
    "partition": "aws",
    "region": "{{output.aws_region.value}}",
    "tags": {
      "name": "{{output.resource_name.value}}"
    },
    "tags_src": [
      {
        "Key": "name",
        "Value": "{{output.resource_name.value}}"
      }
    ],
    "targets": [],
    "tasks": [],
    "window_id": "{{output.resource_id.value}}"
  }
]
